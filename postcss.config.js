// Tailwind v4 is handled by @tailwindcss/vite — do NOT add it here.
// Autoprefixer stays for any non-Tailwind CSS (App.css, third-party CSS).
export default {
  plugins: {
    autoprefixer: {},
  },
};
